# 候鸟实验室 v0.21 开发实施记录

> Canonical设计：[`候鸟v0.21开发方案_重整审核稿.md`](候鸟v0.21开发方案_重整审核稿.md)
> 冻结标识：`V021-DESIGN-FROZEN-2026-08-05`
> 本文只记录实现进度、验证证据和Release Gate，不形成第二套设计口径。

> 2026-08-05用户勘误：ETF Family并非只允许K=1；发布固定K=`1/2/3/5/10/20`，默认四ETF Sample为K=2，且每个Decision执行`K <= floor(rankable / 2)`硬门禁。该勘误覆盖冻结稿原第11.2节的“K=1 only”表述。

> 2026-08-06实验入口修正：Workspace不再把“研究实验”与“Formal提交”合并为一个被Release Gate禁用的动作。正式证据未齐时允许提交`exploratory` Suite，固定使用已发布Data Bundle、静态已选资产集合和5/10bps线性成本；明确不声称PIT成员、terminal event或校准Impact证据，结果不可升级Product。Formal Suite仍由原三项Release Gate确定性阻断。API提交后自动消费持久队列，无需研究者另行启动CLI Worker。

## 阶段状态

| 阶段 | 状态 | 实现摘要 |
|---|---|---|
| 1. 共享契约、Schema、导航与Workspace骨架 | 完成 | v0.21版本边界、11个领域、Workspace编译契约、Product/Queue状态机、两条追加迁移、Workspace/Products导航与页面骨架 |
| 2. Asset、Catalog、Data与Universe | 完成（P0外部门禁保留） | 版本化资产注册表、服务端搜索/分类/能力过滤、canonical序列与CSV接口、新Assets页面已经落地；PIT Universe与新增资产数据接入仍受P0 Gate约束 |
| 3. Factor、Signal与Model选择链 | 完成 | 版本化组件目录、逐层合法性传播、共享Workspace Draft、家族卡/固定参数多选、模型全量输入契约、Factor导出与血缘；配置目录已与旧诊断Artifact解耦 |
| 4. Strategy与Experiment | 完成（Formal Gate保留） | v0.21 Top-K引擎、ETF半数K门禁、六单元固定实验矩阵、持久工作队列、编译预览、服务端实验筛选/分页与完整详情已落地；真实Formal执行受P0门禁约束 |
| 5. Product与Monitoring | 完成（Product Gate保留） | 6/6资格判定、手动Promotion、健康判定、生命周期、OOS监控、告警/复核与Product候选目录/详情均已落地；主环境在证据缺失时拒绝升级 |
| 6. 集成、性能与恢复验证 | 完成（P0 Gate保持关闭） | 本地迁移、真实备份恢复、长队列租约接管、全量静态/单元/集成/前端构建与浏览器闭环验收完成；xhigh按用户要求暂停，待用户前端验收后再决定是否启用 |

## 2026-08-05：阶段1

### 已实现

- 项目版本升级为`0.21.0`；保留v0.2 Catalog与历史Artifact身份。
- 正式领域从9个扩展为11个，新增`workspace`与`product`。
- 新增Workspace纯编译契约：
  - Model必须消费全部已选Signal；
  - 无法入槽、重复匹配、数量不足或溢出均返回稳定reason code；
  - 多Model展开为并行分支，每条普通Strategy只绑定一个Model；
  - Diagnostic-only/Directional输出不能连接首版Top-K；
  - 每个合法Strategy Branch固定生成6个Portfolio Cell；
  - 规范化选择顺序后Fingerprint稳定。
- 新增Work Item、Product Lifecycle、Alert状态机。
- 新增迁移`20260805_29_v021_core`：stable issuer/security、Capability、动态Universe、Partition/Manifest、Missing Observation、Research Draft、Compiled Research Spec/Model/Strategy。
- 新增迁移`20260805_30_v021_product`：持久Work Item、Benchmark Set、Comparison Context、Qualification Bundle、Product Version/Enrollment、Monitoring Snapshot、Alert、Review与append-only事件。
- 前端Workspace替换Research desk；Products替换Compare；Data从导航移除；旧`/data`和`/compare`地址保留兼容入口但不再展示旧页面。

### 验证证据

- v0.2修改前基线：后端`213 passed`，前端`10 passed`，TypeScript通过。
- 新Workspace/生命周期契约：`7 passed`，Ruff通过，strict mypy通过。
- 隔离PostgreSQL测试库：从`20260805_28_v02_target_engine`升级到`20260805_30_v021_product`，完整回滚至旧head后再次升级成功。
- 阶段1全量静态检查：Ruff通过；strict mypy覆盖155个源码文件通过；前端lint、typecheck与production build通过。

### 仍受P0 Gate约束

- 新Schema存在不表示PIT Provider、terminal event、Universe阈值或Impact系数已经关闭。
- 在这些Gate关闭前，相关能力只能标记为Exploratory/Blocked，不得生成Formal Accepted Result或Product资格。

## 2026-08-05：阶段2（资产目录第一批）

### 已实现

- 新增严格资产注册表契约与`assets.v0.21.0.json`：12个分类、89个资产、4个资产集合；名称、symbol、alias与标签全部进入统一搜索文档。
- 明确六级成熟度与`tradable/reference_only/synthetic`边界；当前快照大盘股、展示资产和动态Universe不会因为“出现在目录”而获得正式研究资格。
- 新增迁移`20260805_31_v021_assets`：版本化Asset Registry Release、分类、Security Profile、Asset Set及成员、Terminal Event契约。
- 新增`bootstrap asset-registry`发布命令。集合字段在发布前显式排序，保证相同目录跨进程具有稳定语义。
- 资产API改为服务端搜索、分类、成熟度、可交易属性过滤及分页；返回目录总数、分类计数、资产集合、能力缺口、数据可用性与是否可勾选。
- 新增canonical资产序列与CSV下载接口。下载绑定已发布Dataset Artifact；不存在已发布清洗数据时返回明确不可用，而不是生成空文件或模拟数据。
- Assets前端重做：分类导航、多标签/别名搜索、成熟度过滤、信息卡、合法勾选、灰态能力、资产集合、详情抽屉、复权价格图与清洗CSV下载。
- 前端`selectable`必须同时满足目录研究成熟度、非reference-only和已发布canonical数据；展示能力不等于研究能力。

### 验证证据

- 隔离PostgreSQL测试库从空库迁移至`20260805_31_v021_assets`，11个Schema齐全。
- 资产注册表首次发布成功；第二次发布复用同一Artifact，`reused=true`，语义、内容与Manifest哈希完全一致。
- 跨两个独立Python进程计算规范目录负载，哈希均为`99a785e67f9bb722ac1772fe073e9831d7e7392d970cb9009c6fded55d61de6c`。
- 后端资产契约单元测试`5 passed`；API测试`13 passed`；Ruff及strict mypy通过。
- 前端资产交互测试纳入全套测试，`11 passed`；lint、typecheck与production build通过。

### 尚未关闭

- 当前新增股票与多数ETF尚无canonical历史，因此卡片可展示但不可勾选、不可下载。这是能力事实，不是前端缺陷。
- 正式`us_liquid_large_cap_300_pit_v1`仍需PIT主数据、历史成员、terminal event和流动性/容量证据；在P0 Gate关闭前维持`formal_eligible=false`。
- 价格/总回报、回撤、成交量/ADV、覆盖/缺失等更完整的服务端分析摘要，将随新增canonical数据与Universe管线在阶段2后续批次补齐。

## 2026-08-05：阶段3（Factor、Signal与Model选择链）

### 已实现

- 新增`workspace_contracts.v0.21.0.json`及严格发布契约，将v0.2 Factor/Signal目录与v0.21原始OHLCV Factor、Model Family/Preset合成为不可变Workspace Component Catalog。
- 新增迁移`20260805_32_v021_workspace`及`bootstrap workspace-components`；重复发布复用相同Artifact。
- 新增Workspace Options合法性引擎与API：
  - Factor家族只显示一张卡，固定参数Variant可单选或多选；
  - Signal只有在对应Factor Variant已选且频率兼容时可选；
  - 每个Model默认接收全部已选Signal，任何未接受输入、重复入槽、Slot不足/溢出、频率冲突或未部署实现都会置灰；
  - 不进行自动相关性排斥，保留研究者人工判断；
  - Model Preset可多选，但编译后每个Model与每个Strategy形成独立一对一回测分支。
- Workspace Draft统一持久化Asset、Factor、Signal、Model、Strategy与Frequency选择；上游变化不会静默删除下游选择，失效项保留并显示阻断原因。
- Factors、Signals、Models页面增加搜索、家族卡、固定选择题参数、勾选与灰态；配置目录与旧版历史诊断接口解耦，诊断暂缺不再阻断研究配置。
- 原始Open/High/Low/Close、Adjusted OHLC与Volume已作为Raw Factor进入Factor目录。
- Factor Dataset新增不可变CSV导出；Factor、Signal、Model卡片均提供Artifact血缘入口。

### 验证证据

- 隔离PostgreSQL测试库迁移至`20260805_32_v021_workspace`；Workspace Component Catalog首次发布及重复复用成功，Artifact为`c7f73a08-...`。
- 实际发布目录解析为21个Factor Family、27个Signal Family和4个Model Family。
- Workspace合法性/编译/API等覆盖Factor→Signal→Model传播、全量输入、无静默丢弃、投票/训练约束和Factor导出。
- 阶段3后端全量：`235 passed, 19 skipped`；跳过项均为未设置隔离数据库URL的既有Integration测试。Ruff通过，strict mypy覆盖159个源码文件通过。
- 阶段3前端：typecheck、lint及`11 passed`全部通过。

### 边界

- `directional_vote`只用于Predictive Diagnostic，不允许连接首版Top-K；`lightgbm_ranker`保持planned/disabled，直至训练、PIT与部署Policy全部实现。
- 新资产没有已发布数据时，上游目录仍可浏览，但不得借由组件目录绕过Asset/Data P0 Gate形成Formal Result。

## 2026-08-05：阶段4/5当前批次（Strategy、Experiment与Product）

### 已实现

- Multi-ETF Top-K发布固定选择题`K=1/2/3/5/10/20`；每个Decision执行`K <= floor(rankable ETF数量 / 2)`，四ETF Sample默认`K=2`，两ETF只允许`K=1`。缺分资产退出当期排名，不自动缩小K。
- 新增ETF/大盘股共用的v0.21横截面排序内核，覆盖90%得分率、股票Formal最少100只、边界并列按槽位比例分配、股票buffer/行业上限与内部防御预算。
- 新增固定六单元实验矩阵：全共同历史/近3年/近1年 × 5/10 bps+impact，资金固定USD100M，fresh start且状态重置，并执行5% ADV硬容量门禁。
- 新增数据库迁移`20260805_33_v021_matrix`与可租约、心跳、取消、幂等复用、分类重试的持久工作队列。
- Workspace新增全量编译预览；每个合法Model×Strategy分支生成六个Cell，非法选择返回稳定阻断码，不静默修复。
- Experiments页面采用回测区间、调仓频率、成本和排名指标独立筛选；详情展示净值/基准、超额净值、回撤、完整指标、质量检查与事件。
- Product页面采用研究候选卡片与详情页，展示OOS监控、决策、健康、资格、事件、告警及血缘，明确不等于实盘验证或可交易产品。
- 新增严格六单元Product资格判定、监控健康判定和带revision冲突检测的生命周期服务。
- 实验详情中的升级入口只在完整v0.21 Qualification Bundle合格时启用。现有v0.2单结果及PIT Universe、terminal event、impact policy门禁未关闭的结果均明确不可升级并返回原因码。

### 当前验证证据

- ETF K/Workspace目录与置灰规则专项回归：`15 passed`。
- API、Product资格/监控、Top-K与Workspace Options当前批次回归：`32 passed`；Ruff与strict mypy通过。
- 前端OpenAPI类型重新生成，typecheck、lint及`11 passed`全部通过。

### 仍受P0 Gate约束

- PIT股票Universe历史、terminal/delisting事件和真实impact系数尚未接入，因此系统必须拒绝v0.21 Formal执行与Product升级；代码不得用模拟事实关闭这些门禁。

## 2026-08-06：本地交付设置与复核前验证

- 主数据库从`20260805_28_v02_target_engine`原地升级至`20260805_33_v021_matrix`，11个Schema齐全；既有v0.2发布结果保留。
- 主数据库发布`asset_registry v0.21.0`与`workspace_components v0.21.2`，API健康检查返回`system_version=0.21.0`及最新迁移头。
- 修复4处集成验收陈旧假设：旧迁移头断言更新为v0.21，Scope测试显式发布v0.21 Asset Registry后再验证Assets API。
- 后端：Ruff通过、strict mypy覆盖167个源码文件通过、`255 passed`；数据库Integration独立复跑`20 passed`。
- 前端：OpenAPI类型生成、typecheck、lint、`11 passed`及production build全部通过。
- 应用浏览器实际检查Workspace、Assets、Factors、Signals、Models、Strategies、Experiments、Products；未发现前端console error。Workspace显示默认四ETF、Weekly、K=2、六单元矩阵，Experiments可读取既有9,450条历史Cell。
- 根README与`.env.example`同步至v0.21启动说明和版本号。
- 本地服务已运行于`http://127.0.0.1:8000/`，用于后续xhigh人工与代码复核。

## 2026-08-06：剩余闭环开发重新启动

- 修正“前端页面存在即基本完成”的过早判断，重新以Draft→Suite→Work Item→Result→Qualification→Product→Monitoring的可执行闭环验收。
- Workspace Draft已写入`workspace.research_draft`，支持revision乐观锁、加载恢复和冲突409；页面不再只保存浏览器内存状态。
- 新增确定性的Release Gate API。PIT Universe、terminal event和impact policy证据未发布时，Formal与Product能力均fail-closed，并返回稳定reason code。
- 新增迁移`20260806_34_v021_model`，Compiled Model Instance正式保存全部参数与训练target，避免不同Preset/target在下游身份碰撞。
- 新增Research Suite发布服务与API：发布Compiled Spec/Model/Strategy、版本化Execution Policy、Suite、Predictive Cell和固定六格Portfolio Cell，并为每个Cell幂等入队；Suite可汇总状态并整套取消。
- Workspace增加保存Draft、Compile Preview和Create Formal Suite入口；门禁未关闭时按钮确定性禁用，后端即使被直接调用也返回结构化409，不生成伪Formal Artifact。
- Suite服务已在真实隔离PostgreSQL中验证：单Model×单Strategy生成`1+6=7`个任务；重复提交复用同一Suite；整套取消后7个任务全部进入终态。
- 独立Worker与结果物化已经完成：按Work Type调用可替换执行器，分类记录失败与取消，并在单一事务中原子写入Result Artifact、`experiment.cell_result`和Work Item终态。P0门禁未关闭时不注册伪金融计算器。
- Qualification/Promotion闭环已经完成：只接受同一Suite、同一Strategy与同一Comparison Context下完整且已发布的6/6 Portfolio Cells；升级时原子写入Qualification Bundle、Product Version、Product Enrollment、默认Monitoring Policy与首个生命周期事件。
- Experiment详情页从真实资格API读取状态；合格时通过弹窗填写候选名称、选择理由和备注后升级，并跳转Product详情。既有v0.2单结果不会被误判为完整资格包。
- Product Monitoring闭环已经完成：新发布Data Bundle可为所有Active Enrollment幂等调度监控任务；Worker原子追加Monitoring Snapshot、更新健康状态、创建告警并完成任务。告警支持确认、解决和取代，所有变化均保留事件历史。
- Product详情页已加入带revision乐观锁的暂停、恢复和退役操作；生命周期变更必须填写理由和明确生效时间，退役不会被静默回填。
- 新增迁移`20260806_35_v021_result`与`20260806_36_v021_monitor`；主数据库已升级至最终迁移头，API暴露受控写命令并保持非本地绑定拒绝策略。

### 最终闭环验证

- 真实隔离PostgreSQL验证完整路径：发布7个Cell任务、Worker物化结果、构造版本化Comparison Context、形成严格6/6资格包并升级Product；随后调度监控、写入Warning Snapshot、打开并确认告警、完成Product退役revision更新。
- 后端最终验证：Ruff通过；strict mypy覆盖175个源码文件通过；非Integration测试`255 passed`；隔离PostgreSQL全量Integration测试`24 passed`。
- 前端最终验证：重新生成OpenAPI类型，typecheck、lint、`11 passed`和production build全部通过。
- 外部P0门禁仍然保持打开：PIT股票Universe历史、terminal/delisting事件与校准impact系数尚未接入。因而正式Suite执行与Product升级在当前真实主环境中继续被确定性阻断；这是有意的发布安全边界，不是未完成的前端功能。

## 2026-08-06：xhigh首轮审计整改闭环

- 新增真实v0.21数据库执行器与CLI Worker：Predictive Cell按冻结Signal集合形成Model分数；Portfolio Cell执行Top-K、调仓日映射、100M容量、5% ADV、冲击成本、5/10bps、SPY基准和净值/指标物化。Worker对执行器输出实施系统级质量门禁，测试替身不能自行声明合格。
- Experiments API与页面已读取v0.21 `cell_result`，支持独立筛选、排名和完整结果详情；Qualification冻结精确6个Result Artifact、6个Cell Artifact、完整Selection Context和依赖血缘。
- Release Gate改为数据库中的版本化证据；PIT、terminal event与impact policy缺少任一有效已发布证据时均fail-closed，关闭门禁必须绑定精确Comparison Context。
- Queue可回收过期租约，取消/失败Suite重提会重新绑定Work Item；基础Worker与Monitoring Worker对Infrastructure/Interrupted失败执行有界重试。
- Product升级幂等返回真实Enrollment；已退役候选可重新登记为新Enrollment。未来生效的生命周期请求不会提前改变当前状态，Suspended候选继续估值但冻结持仓。
- OOS从激活后的首个合法Decision开始，只接受更新且覆盖日严格匹配的已发布Data Bundle；内部SMA200防御、固定20%防御和无防御均按冻结参数执行。
- 所有写命令要求幂等键；相同键与相同请求返回首次结果，不同请求复用同一键返回409。Monitoring Snapshot、Alert Event为数据库强制append-only，Lifecycle Event只允许`applied_at`从空值完成一次性生效标记。
- Workspace首次无草稿时真正载入IWF/IWD/IWO/IWN、周频、K=2样例；支持防抖自动保存、复制与最后编译对象记录。Product详情展示真实OOS持仓和Decision信息。
- 最终验证基线更新为：Ruff、strict mypy、后端`256 passed`非Integration、隔离PostgreSQL `25 passed`（含并发幂等与租约恢复）、前端typecheck/lint/`11 passed`/production build全部通过；迁移头为`20260806_41_v021_append_only`。

## 2026-08-06：xhigh二轮审计整改与交付收口

- Portfolio执行改为周/月末合法Decision、下一共同Session原始开盘成交；100M Currency NAV逐期演化并进入订单金额、严格前20日ADV、5%参与率、平方根冲击、5/10bps与独立SPY基准路径。Capacity Rejected保留Gross NAV与逐笔容量证据。
- 全资产Accounting Grid显式区分上市前零权重占位、真实缺失与已确认terminal value；PIT资格、行业分类、terminal event和防御篮子均消费Execution Policy冻结的Release Gate Artifact，不再读取运行时“最新”。
- Predictive执行在共同有效资产-日期集合内按分支重新归一化，逐点保存原Signal、归一化输入和贡献；冻结Forward Return Target后计算逐期Rank IC、目标覆盖率与非退化率，实验页单独展示预测诊断。
- 系统级结果门禁独立复核分数贡献、横截面非退化、Target覆盖、NAV/Currency NAV/Excess对账、Decision→Execution时序与逐笔容量；执行器自报`passed`不能绕过门禁。
- Product OOS改为两阶段状态机：合法Decision只产生Pending Target，下一Session原始开盘才执行；其余Session只估值。暂停会清除未成交旧目标且继续漂移估值；月度绩效复核与每次预测Decision的退化连续计数分离。
- Product详情增加Primary/Stress/SPY/Excess和回撤图、持仓/成本容量/预测证据、告警、只追加月度复核及完整血缘；Review表由数据库append-only触发器保护。
- 新增迁移`20260806_42_v021_oos`并将主库升级到该头；OpenAPI和前端类型同步。
- Asset Registry升级为`v0.21.1`：依据Nasdaq官方`NDX`成分文件（数据日期2026-05-01）补充展示候选，股票由20只扩至112只、总目录181项。新增股票保持`cataloged`并显式缺少PIT主数据、terminal event和canonical history，不能进入Formal链路。
- 最终自动验证：Ruff通过、strict mypy覆盖179个源码文件；Python Unit `256 passed`；独立PostgreSQL Integration `25 passed`；前端typecheck、lint、`11 passed`与production build全部通过。真实v0.21 API额外验证7个Cell（1 Predictive+6 Portfolio）总览及Predictive详情可读。

## 2026-08-06：xhigh最终验收修正

- 修正Predictive Target覆盖率口径：Forward Return Dataset只包含冻结目标频率规定的周末/月末Decision Date，而Signal/Model分数可以每日发布；因此覆盖率分母必须是冻结Target Dataset内的应评估Decision Date集合，不能使用全部每日Model Score日期。
- `V021DatabaseExecutor.execute_predictive`现在只遍历Target Decision Date并计算`aligned_target_period_count / target_period_count`；每日非目标日期不再错误拉低周频/月频覆盖率。
- 新增真实执行器回归：182个每日分数日、26个周频Target Period必须得到`26/26`、覆盖率`1.0`和`accepted`，用于防止所有生产Predictive Cell被系统性误判为`data_quality_failed`。

## 2026-08-06：暂停xhigh审计，转入用户可见功能核对

- 按用户要求暂停继续xhigh审计，先核对前端与实际数据能力，不再把“页面已存在”视为开发完成。
- 新增个股当前只有`asset_registry v0.21.1`目录身份，尚无canonical历史行情、PIT历史成分、退市终值与可发布Factor/Signal Dataset；因此卡片可展示/搜索但不可勾选、下载或进入正式个股回测。这属于剩余数据工程，不是单纯前端缺陷。
- 旧v0.2实验结果实际仍支持详情API与图表；浏览器点击后URL会加入`result=<artifact_id>`且详情成功加载。但详情被渲染在长实验列表之后，点击不会自动滚动或打开抽屉，用户视觉上会误以为“无法点开”。这是确认的实验页交互缺陷，与是否已运行v0.21新回测无关。
- 旧v0.2结果只允许查看兼容详情，不满足v0.21的1 Predictive + 6 Portfolio、冻结Gate和Qualification身份，不能升级为v0.21 Product；新的预测诊断区为空则是因为主环境尚未发布真实v0.21结果。
- 当前状态定义为“核心框架基本完成、完整交付未完成”。恢复开发时优先级为：个股/扩展ETF数据接入与PIT Universe；实验详情抽屉/自动定位和分页或虚拟化；raw OHLCV Factor Dataset导出/血缘；真实v0.21端到端回测；之后再恢复xhigh逻辑审计。

## 2026-08-06：用户可见功能第一轮收口

### 验收方式调整

- 暂停xhigh审计。后续固定采用“Codex实现与自动测试 → 用户从前端逐页人工验收 → 用户确认主要功能后再启用xhigh查隐藏逻辑问题”的顺序。
- xhigh不再替代产品功能验收；本轮只处理用户能直接观察到的缺口。

### 扩展资产数据链

- 新增版本化`v021_asset_identity`发布物，将Asset Registry中164个`tradable + XNYS` Security桥接到canonical daily-bar链使用的稳定Asset、Listing和Ticker身份；原5个v0.2资产复用既有身份。
- 新增批量Yahoo快照能力：每批最多40个symbol并行获取，但每个资产仍发布独立、不可变的原始快照；增加`BRK.B → BRK-B` Provider符号映射，目录与CSV继续保留正式symbol `BRK.B`。
- 已实际获取并发布164个标的、2006-01-01至2026-08-05的独立原始快照；发布新的XNYS Calendar v2（2006-08-07至2026-08-05）。
- canonical解析按已确认的新缺失契约执行：单点OHLC缺值和正常交易日缺口记录Warning并使该资产当日缺失，不再拖垮整个资产池；OHLC几何、负价格和真实非交易日记录仍为硬错误。
- 已实际发布canonical market Artifact `d1392990-ef1f-4bca-bc21-71deb4e12e3a`，覆盖164个标的和755,729条有效日线。浏览器实测AAPL为2006-01-03至2026-08-05、5,179条观测，图表和清洗CSV均可读取。
- Asset API在canonical数据存在时动态移除`canonical_history`缺口；拥有数据且目标成熟度为研究/策略输入的tradable资产可以勾选进入探索性配置。股票Formal仍由PIT Universe、terminal event等Release Gate阻断，普通Yahoo历史不会冒充PIT证据。

### Factor、Signal、Model与Strategy前端收口

- Factors移除v0.2因子实例分布、相关性和诊断问题长清单；21个因子家族各显示一张卡，公式、输入字段、输出单位、时间语义、计算实现和全部固定参数集中在卡内。原始OHLCV因子保留。
- Signals改为可展开家族卡；补齐27个Signal家族的中文名称与经济解释，并展示方向、输出形式、依据类别、研究层级及产品研究资格。展开后按Factor Variant选择版本。
- 新增`GET /api/v2/signals/versions/{version_key}/download.csv`；只导出该固定Signal Version的已发布值，并返回Artifact Content Hash。浏览器选中版本后显示“下载信号CSV”；实际接口验证返回200及831,077字节CSV。
- Models补齐四个模型家族的中文名称与解释，展示参数、输入槽、输出可比性和全部接受的上游信号。当前统一展示target为“已选择资产组合中，各资产在下一调仓周期的未来盈利能力”；训练型模型保留具体target参数身份。模型不提供数据导出。
- Strategies移除v0.2规则、产品和已发布Target Path三块长清单；两个Top-K家族卡直接展示启动/正式资产门槛、覆盖率、频率、主/研究基准、全部固定参数范围与每个可选Preset。
- Experiments此前已改为50条分页与固定右侧详情抽屉；旧v0.2结果可以查看详情，但不能冒充v0.21六格资格包。

## 2026-08-06：应用闭环、恢复与性能验收收口

### Workspace、Experiment与Product闭环

- 重新以当前工作树和真实数据库为证据审计`Draft → Suite → Work Item → Result → Qualification → Product → Monitoring`，不再把已有类名或页面当作完成证明。
- 主环境Workspace草稿`local/default`可跨刷新恢复精确选择与revision；当前为v1。编译、保存、乐观锁、幂等提交、Suite状态与整套取消均有API和PostgreSQL集成覆盖。
- 隔离PostgreSQL完整回归验证1个Predictive加6个Portfolio任务、租约接管、失败重试、并发命令幂等、严格6/6资格、原子Promotion、监控调度、Snapshot、Alert、Review及Suspend/Resume/Retire。
- 主环境Release Gate继续返回`pit_universe_gate_open`、`terminal_event_gate_open`、`impact_policy_gate_open`，同时关闭Formal与Product；Workspace按钮禁用，实验旧结果的Promotion按钮禁用，后端直接调用同样fail-closed。

### 性能与恢复证据

- 真实创建PostgreSQL custom-format测试库备份`artifacts/v021-test-recovery.dump`，大小788,553字节、SHA-256为`30def976160eb3cad2815d8a5e29bb67dd98817db0ec99d9e69c770f568e42ce`；已恢复到隔离临时数据库、核对迁移头并将记录状态更新为`restore_tested`，临时恢复库随后清理。
- 在隔离库插入10,000个持久Work Item进行Claim压力检查；20次Claim的p50为10.15ms、p95为12.24ms、最大12.50ms，过期租约在长队列中由另一Worker成功接管。测试标记数据随后从隔离库精确清理。
- 修复Experiments“前端分页、后端全量加载”的伪分页。API现在按区间、频率、成本、状态和排名指标服务端过滤/排序，稳定`limit/offset`分页，并单独返回总数和状态计数。
- 主库9,450条历史Cell下，默认筛选315条、返回50条；运行API热请求p50约264.7ms、最大约275.6ms、响应约57.6KB。修复前全量响应热请求p50约9.6秒。

### 浏览器发现并修复的可见缺陷

- 实验详情中的Decision Explorer此前因响应漏定义`dimension_transform`而显示大量Pydantic校验错误；响应契约现已补齐该字段。
- 同时修复旧v0.2决策血脉在贡献计算中通过`pop()`删除该字段的问题，保证v0.2历史Result继续可读；对应完整因子→实验→决策血脉集成测试通过。
- 浏览器最终复验：实验第2页恰好50条并显示`2 / 7 · 315 results`；详情抽屉、净值/超额/回撤图、完整指标、质量检查、运行事件和单次决策解释器均正常；Workspace草稿和Gate缺口正常；Products明确为Research Candidate且当前为空；控制台无error。

### 最终验证基线

- Ruff通过；strict mypy覆盖180个源码文件通过。
- Python非Integration：`259 passed`；隔离PostgreSQL Integration：`25 passed`。
- 前端typecheck、lint、`11 passed`及production build全部通过。
- 主数据库迁移头`20260806_43_v021_exploratory`，本地API已重启并运行于`http://127.0.0.1:8000/`。

### 2026-08-06：探索性实验入口与后续两项设计议题

- 新增迁移`20260806_43_v021_exploratory`，`Research Suite`身份显式冻结`formal/exploratory`，旧Suite默认保持`formal`。
- 默认四ETF草稿实测可编译：4/4资产、1个Predictive Cell、6个Portfolio Cell；探索性提交实测7/7 Work Item完成。
- 四ETF Sample在USD100M与5% ADV硬容量约束下的六个Portfolio Cell均为`capacity_rejected`，这是冻结容量合同的预期结果，不伪装成Net Accepted；详情回退展示拒绝前Gross财富/回撤诊断。其后用户决定允许此类结果带“不满足100M容量”强警告进入Research Candidate观察，见最终收口记录。
- Experiments默认排序优先显示新v0.21 Suite，避免最新研究被9,450条v0.2历史Cell淹没；探索性结果显示身份标签。
- 待本轮实验入口验收后单独设计、再与用户确认：
  1. Model Target增加`Future Return`（带timeframe选择）与`Relative Future Return = Future Return - 当期Universe Mean`；二者是不同Model输出身份，横截面排序默认倾向Relative Target。
  2. Signal层增加批量导出：一次导出当前已选资产×已选因子/信号的长表数据；首选Parquet，并需冻结列模式、日期范围、缺失语义、Artifact/版本清单和大文件异步任务契约。
- 本轮不启动xhigh；按用户确认的顺序，下一步由用户逐页人工验收可见功能，再决定是否进行隐藏逻辑审查。

### 2026-08-06：Product候选门禁降级与Model Target方案（已确认）

- 外部门禁缺口不是同一种问题：股票PIT Universe缺少历史成员、历史市值/股本及issuer/security映射；terminal event缺少可靠退市、收购、换股和破产终值资料；Impact已有5/10bps线性成本与5% ADV容量诊断，但缺少可审计的真实执行样本和校准系数。
- 保留三项Release Gate，不再把它们作为“登记Research Candidate”的统一硬阻断；它们继续阻断`Formal/Deployable`声明。Exploratory、non-PIT、terminal coverage不完整、impact未校准或容量拒绝的结果，在其余计算与数据质量检查通过后可手动登记为Product研究候选，但必须携带不可移除的风险标签、警告卡与对应证据缺口。
- Product页面增加“鸟蛋/孵化中”身份：明确表示仅进入激活后样本外观察，不代表PIT无偏、100M可部署、成本已校准或已经实盘验证。后续证据补齐时再逐项解除警告，不改写历史版本。
- Target冻结为两个`target_kind`：`future_return`和`cross_sectional_relative_return`。后者定义为同一Decision、同一Horizon下，资产Future Return减去当期目标有效Universe的等权均值；目标缺失的资产既不填充Target，也不进入当期均值。
- Horizon只允许预设选择，不允许手输；首组选项为5、21、63个共同交易Session（界面显示1周、1月、3月）。收益均按Decision之后的下一共同开盘进入，并在对应Horizon的共同开盘退出。外部模型研究也使用相同标签定义，以便训练产物无歧义接回候鸟。
- 每个`target_kind × horizon`形成独立Model Instance/输出身份。确定性模型的Target只影响预测诊断；训练型模型的Target进入训练契约、purge/embargo与模型身份。
- 重要语义：同一期横截面内减去Universe Mean不会改变Future Return的排序或Spearman Rank IC；Relative Target的主要价值是去除共同市场分量、改变训练损失与跨期可比性，而不是改变当期真实收益名次。

### Signal全量研究数据导出（已确认并实施）

- 用户确认导出严格遵守`Asset → Factor → Signal → Model`研究链：只在Signal层导出当前已选Signal Version的数值面板，不导出资产层原始行情、未选择数据字段、Factor值或其他“完整特征库”。上游选择的意义必须保留，资产池扩大不能导致无关列和垃圾数据进入模型研究包。
- Signal层提供“导出全部已选信号”按钮；导出范围为当前已选资产×当前已选Signal Version。资产标识与日期仅作为行键/元数据，不属于模型输入；所有Signal参数实例保持独立稳定列。
- Parquet使用宽表与Zstandard压缩；主键为`date/decision_date + asset_id`。Signal缺失值保持`null`，不补零、不跨资产填充，也不因局部缺失删除整行。
- 大文件通过异步Export Job生成；完成后下载Signal Parquet与Manifest/Data Dictionary组成的单个ZIP研究包。Manifest仍冻结Asset Selection、Signal Version、上游Lineage、Data Bundle、Artifact ID、Content Hash、日历、时间区间、缺失政策与non-PIT警告，但上游Factor数值不进入数据文件。
- Asset分类内增加“全选当前分类”；Factor/Signal家族继续支持参数实例多选。资产层和因子层不增加批量数据导出入口，避免重复能力与边界混乱。
- 已确认：外部监督学习所需Target作为独立`targets.parquet`附带。Target不是Model输入，也不是Factor/Signal原始数据，与Signal列物理/语义分区，防止误当特征和未来信息泄漏。

### 基本面数据链（设计已确认，真实Provider待后续数据工程）

- 支持引入基本面数据，但资产层应保存带`period_end`、`filed/accepted_at`、`available_at`、币种、单位、来源与修订版本的原始财务事实；禁止按财报期末提前回填，所有下游计算只可使用Decision时点已经公开的数据。
- 资产详情可查看各类数据能力和覆盖率；Workspace建议按所选资产集合统一勾选数据系列，而不是逐只股票形成不同的数据选择。对没有该系列的资产保留局部缺失，并由下游Factor/Signal卡显示覆盖率和合法性。
- 优先接入组成比率的Canonical事实，例如收入、净利润、每股收益、股东权益、总资产、总负债和流通股本；PE与ROE在Factor层由明确公式自行计算。Provider直接给出的Ratio可展示或校验，但不应成为唯一研究事实。
- 首批Factor可包括Earnings Yield（优先于直接用PE排序）、ROE、Revenue Growth、Earnings Growth与Leverage；下游分别形成Value、Profitability/Quality、Growth和Balance-Sheet Risk信号家族。
- 非正EPS时PE不应被解释为“极度便宜”；负/接近零权益时ROE也必须缺失或触发专门状态。季度数据按`available_at`做as-of join并携带数据年龄，不能把后来重述的数据写回过去。
- ETF若要计算基本面指标，需要PIT持仓与权重；没有该数据时，股票基本面链可用，ETF基本面只能标为当前快照/展示数据，不能冒充历史可研究输入。

## 2026-08-06：v0.21最终一轮开发与浏览器验收

- Product Promotion切换到Research Candidate资格：完整六格和身份一致仍为硬门，Exploratory、non-PIT、terminal coverage、uncalibrated impact及Capacity Rejected改为警告。修复Capacity Rejected带warning quality时被误判为失败的问题。
- 候选OOS监控兼容探索性Execution Policy：无正式Gate时使用静态已选Universe；impact禁用时只计线性5/10bps，不读取或伪造ADV冲击参数。Product卡片与详情加入鸟蛋“孵化中”和警告面板。
- Workspace/Model加入两种Target Kind与5/21/63 Session多选；编译器按Model Preset×Target预设生成独立Model Instance。真实Signal研究包验证Target按单独Parquet输出。
- Signal层加入“导出全部已选信号”：实际以四ETF×1个Signal导出ZIP，`signals.parquet`为19,104行、`targets.parquet`为4,168行，并包含Manifest与Data Dictionary；未混入Factor/OHLCV列。
- Asset详情加入Data Inputs区。已发布canonical OHLCV显示为可用；股票Filed Fundamentals及PE/ROE下游Factor明确显示为孵化中/禁用，等待带filed/accepted/available时点的真实Provider，不用当前比率伪造历史。
- 浏览器逐页复验Assets、Models、Signals与Experiments。修复v0.21 Decision Explorer漏返回`dimension_transform`以及探索性实验详情仍写死“不可升级”的问题；容量拒绝六格现在显示可升级为样本外研究候选，警告完整，决策血缘可展开。
- 验证：Python Unit 262项通过；Ruff与strict mypy（181源码文件）通过；前端lint/typecheck、11项测试和production build通过。真实PostgreSQL Integration其余24项通过，唯一草稿旧契约夹具修正后定向复验通过，合计25/25通过。

## 2026-08-06：Workspace草稿同步与中文乱码修复

- 根因确认：研究选择保存在全局Context与浏览器本地草稿中，但旧Workspace页面每次重新挂载都会再次读取服务器草稿并无条件`replace`，从而以旧Revision覆盖用户刚在Assets等子页面完成的选择。Autosave又只挂载在Workspace页面，形成“子页面仍能看到、回工作台却归零”的竞态。
- 草稿初始化与自动保存已提升到常驻的`WorkspaceSelectionProvider`：应用启动时只水合一次；存在浏览器本地快照时优先保留并与服务器比较；任意研究层修改均在900ms后写入服务器，而不再等返回Workspace。保存请求只在其快照仍为当前状态时清除Dirty标记，避免在保存期间的新修改被误报为已保存。
- Workspace不再负责服务器水合或页面级Autosave，只展示统一同步状态，并保留“保存草稿”即时触发按钮。清空、多选、参数切换同样进入统一持久化流程。
- 全面移除Workspace源码中遗留的错误编码文本，恢复研究链箭头、门禁说明、实验按钮、分隔点和乘号；同时补齐控制区中文文案。
- 新增前端回归用例，覆盖`Assets勾选 → 返回Workspace → 应用重新挂载`后资产数量仍为1。前端lint、typecheck、12项测试和production build通过。
- 真实浏览器验证：基线4个ETF上临时勾选AAPL后，返回Workspace资产计数为5且显示“已保存”；刷新后仍为5，页面未发现乱码或控制台警告/错误。验证结束后取消AAPL并确认计数恢复为4、草稿已保存。本地服务运行于`http://127.0.0.1:8000/`。

## 2026-08-06：长目录交互优化与股票研究链缺口确认

- Asset详情过去渲染在带页面入场Transform的`.page`祖先内，导致`position: fixed`实际以长文档容器为定位基准；用户在列表中段打开时先看到抽屉下半段，必须回到页面顶部才能看到详情。现改为通过Portal直接挂载到`document.body`，抽屉固定覆盖当前视口、独立滚动，并在打开期间锁定背景滚动。真实浏览器从LLY卡片打开后，抽屉从当前视口顶部完整显示。
- Factor、Signal、Model、Strategy及Workspace的合法选项查询过去在每次勾选后更换Query Key并短暂卸载整页，浏览器因此回到初始滚动位置。现统一保留上一份Query Data，后台返回新合法性后原位更新，不再用整页Loading替换卡片目录。
- Asset卡片的`数据可用`改为更精确的`行情可用`；它只表示Canonical OHLCV已发布，不再暗示Factor、Signal或Strategy已经就绪。Signal导出无覆盖时提供中文说明，Strategy不可选Reason Code补充中文映射。
- 股票问题实测结论：当前草稿为112只Stocks、4个Factor Variant、3个Signal Version及1个合法线性模型。112只资产均有Canonical行情；但三个所选Signal的已发布Dataset各自只覆盖旧四ETF（IWF/IWD/IWO/IWN），所以股票与Signal Value交集为0。Signal批量导出因此正确拒绝，而不是少数股票缺失造成。
- 个股Top-K当前不可选也不是模型或K参数错误：模型合法并完整接受3个Signal；策略返回`selected_asset_unavailable / asset_count_below_launch_minimum / rankable_count_below_k`，因为股票Registry当前成熟度仍为`cataloged`，且尚无覆盖股票的已发布Factor/Signal研究链。不能仅取消前端禁用，否则实验Worker仍会因Exact Published Signal Dataset缺失而失败。
- 用户口述的“动量、偏度、峰度三个信号”与实际草稿存在一个细节差异：当前选择是两个动量Signal（W20/W60）和一个低偏度Signal；峰度只勾选了Factor `return_excess_kurtosis__w120`，尚未勾选对应`low_kurtosis_quality` Signal。
- 下一正确修复不是放宽门禁，而是发布一个明确标注Current-Snapshot/non-PIT的股票探索Universe，为所选资产按需物化Factor与Signal Dataset，并使导出与Suite绑定该精确Bundle/Universe；完成后再把对应Registry成熟度提升到Research Ready。该工作属于研究数据发布阶段，不能用前端状态伪装。
- 多标签草稿同步额外修复：本地快照现在持久化服务器Revision；服务器Revision更新时服务器胜出，同Revision下才保留尚未Autosave的本地修改。旧格式无Revision快照首次加载时以服务器为准，防止旧标签覆盖新研究。浏览器复验中发现的测试污染已按原Revision 54内容恢复为Revision 56：112资产、4因子、3信号、1模型，未丢失用户选择。
- 验证：前端lint、typecheck、13项测试和production build通过；新增Portal归属、勾选不卸载页面、刷新持久化及“新服务器Revision覆盖旧浏览器快照”回归检查。

## 2026-08-06：Workspace按需研究数据流（取代逐层预发布方案）

- 冻结原则调整为：Workspace传递的是可计算的研究配置；Factor、Signal与Model数值按需物化并进行内容寻址缓存；只有导出、实验和Product/OOS边界生成不可变研究快照。该原则取代上一节“先为股票发布完整Factor/Signal Dataset再允许研究”的临时结论。
- 新增统一`WorkspaceSignalMaterializer`。它从所选Security映射到Canonical Asset，绑定最新且完整覆盖所选资产的已发布行情数据集，按Signal血缘解析Factor Variant与计算实现，只计算当前选择需要的Factor和Signal；不把未选资产、未选Factor或未选Signal塞入研究包。
- 缺失政策改为局部缺失保留：Factor暖机不足、上市较晚或局部行情缺失时，该资产对应日期不产生数值；横截面Signal仅在当日有效资产集合内重新归一化，不补零、不跨资产填充，也不因一个资产缺失使整个Universe失败。事件Signal没有紧邻有效前值时保留缺口，不伪造穿越事件。
- 内容寻址键冻结Data Bundle内容哈希、已选Asset ID集合、Signal/Factor Artifact哈希、频率、缺失政策与物化器版本。大矩阵保存为Zstandard Parquet，Manifest保存语义和行数；迁移`20260806_44_v021_materialization`只在数据库索引缓存哈希、Bundle血缘、存储位置、行数和访问时间。相同输入直接复用缓存。传统`factor_value/signal_value`表继续服务Canonical发布，不再承担每次Workspace点击产生的临时快照。
- 若最新Canonical行情数据集尚未进入Data Bundle，系统只在Signal导出或Experiment提交边界自动发布一次不可变Bundle，并复用现有Calendar、Rate与Reserve成员；卡片勾选和Compile Preview不触发重计算或数据库快照。
- Signal批量导出与v0.21 Experiment Worker现共用同一个物化入口。探索性Suite不再要求预先存在Exact Published Signal Dataset或旧Forward Return Dataset；训练Target继续从冻结Bundle按5/21/63 Session即时计算，并与Signal特征分文件输出。
- Asset/Strategy合法性改为“Canonical行情可计算且目标成熟度允许研究”，不再误用尚未提升的当前Registry成熟度。股票Universe中的ADR与Common Stock共享equity执行契约，允许共同进入大盘股Top-K；本轮实际选择109只Common Stock与3只ADR后，112只均可用，18个无PIT行业上限的个股Top-K预设可选。
- 真实验收：当前Revision 57草稿的112只股票、3个Signal首次按需计算得到1,516,538个Signal值，并建立4.58MB内容寻址Parquet缓存；包含6组Target的ZIP导出成功（HTTP 200，7.91MB）。系统同时冻结覆盖164个资产、2006-08-07至2026-08-03的Data Bundle v6；二次Signal-only导出命中缓存并成功返回。

## 2026-08-06：探索性实验提交反馈与自动跳转

- 用户看到“运行探索性实验”点击后无变化，实测确认请求确实已经发往后端并创建Suite；首轮创建约需二十余秒，旧页面只把按钮静默禁用，响应后仍停留在Workspace，因此视觉上等同失效。
- Workspace按钮在请求期间明确显示“正在创建实验…”，并禁止重复提交；提交失败继续在控制区下方显示可重试错误，不再静默吞掉状态。
- Suite创建成功后自动跳转到`/experiments?suite=<research_suite_id>`。Experiments页读取该Suite的真实队列状态，每2秒刷新一次，显示已结束任务数与总任务数；完成后自动刷新预测诊断和策略结果目录。
- 真实浏览器复验：点击后出现创建中状态，随后自动进入Experiments；新Suite `b1e2a957-8d4c-4b92-adcf-e6ab8a6f1ffb`显示“实验已提交，正在计算”及`0 / 13`进度。前端lint、typecheck、14项测试和production build通过。

## 2026-08-09：Product精确留存、常驻Worker与存储维护

- 按用户最终确认，Product长期保留的是“可持续生成信号和解释研究结论的完整证据子图”，不是整个来源Suite：固定六格Portfolio Cell/Result、所选模型对应Predictive Cell/Result、Asset→Factor→Signal→Model→Strategy配置链、Data Bundle/日历/Universe/执行与基准政策、Qualification/Product身份及全部后续OOS记录均被精确pin。
- 同一Suite中未被Product选中的兄弟策略、模型分支、结果和工作项不再被连带永久保留。来源Suite根身份只作为审计锚点保留，不授予整套内容永久保留权。
- 实验清理改为租约安全的两阶段流程：queued任务原子取消，running/leased任务只请求取消；达到终态后由队列drain或常驻Worker空闲维护再次回收。Suite generation advisory guard避免“新提交与旧批次GC”竞态。
- 实验与Product Monitoring Worker已拆为两个隐藏常驻进程，API启动脚本会幂等确保二者存在；API重启不再遗留永久queued任务，实验drain取消10,000项上限。
- Monitoring队首规则允许failed/cancelled/reused终态继续推进，首个`monitoring_start_at`、`first_decision_at`与首个Snapshot在同一事务成功时写入，失败入队不再伪造监控起点。
- Predictive资格与健康参考只绑定Product实际使用的Compiled Model Instance及Target，禁止同Suite其他模型污染阈值；Product最新研究建议只使用冻结Data Bundle内精确Trading Calendar Version。
- Workspace按需物化结果新增`workspace_signal_materialization`轻量Artifact，准确冻结Bundle、Signal/Factor Version、物化器版本和cache hash；Predictive Result依赖该Materialization Artifact，不再把Signal Version冒充Signal Dataset。
- Product-aware存储维护服务已实现只读dry-run、14天TTL/10GiB LRU缓存计划、引用重验、隔离回滚、Zstd Parquet冷归档、逐Manifest hash与整包SHA-256恢复验证。CLI入口为`style-rotation storage retention-dry-run`。
- 主库只读dry-run显示：Product证据pin 223个Manifest；当前实验13个；36,577个旧实验Manifest约1.368GB可进入冷归档候选；共享/未分类1,315个约6.38MB继续保留；当前缓存9.17MB无可回收项。本轮严格未执行真实删除、归档写入或数据库压缩。

**精确留存实现信号：`V021-PRODUCT-EXACT-RETENTION-2026-08-09`。**

## 2026-08-09：资产级数据输入选择契约

- Workspace Selection新增`asset_data_inputs`，按稳定Security ID冻结每只已选资产实际进入下游的数据输入。资产卡批量勾选自动带入已发布的`canonical_market_bars`；资产详情允许对可用且可选的数据源逐项多选，计划中的PIT财务申报数据继续禁用。
- 旧API请求、旧PostgreSQL草稿与旧浏览器草稿在完全缺少该字段时迁移为每只已选资产使用`canonical_market_bars`。显式空列表代表研究者确实取消行情输入，不会被默认值静默修复；草稿校验要求映射键与已选资产严格一致。
- Workspace Options、Compile Preview、Asset Context指纹、Suite `normalized_selection`和Product后续配置血缘均携带精确输入选择。当前Factor目录显式要求`canonical_market_bars`；缺少时Factor和依赖Signal变灰，Model/Strategy随之失效并返回确定性Blocker。
- Signal研究包请求携带同一资产输入映射；服务端再次核验输入已发布且资产可研究，并在Manifest的每个资产条目记录`selected_data_inputs`。未选行情或试图选择仍在孵化的输入会返回422，不会绕过Workspace契约计算。
- Assets页面改为自动读取全部API分页，再渲染和执行“全选当前筛选结果”；扩展资产池超过200项后不会静默漏选第二页及以后资产。
- 回归覆盖旧契约迁移、显式空选择、未发布输入阻断、Compile Preview、资产详情持久化和201项分页；定向Python单元/集成、Ruff、strict mypy及前端typecheck/lint/test均通过。

**资产输入契约实现信号：`V021-ASSET-DATA-INPUT-SELECTION-2026-08-09`。**

## 2026-08-09：Signal研究包持久异步导出

- Signal批量导出从同步HTTP生成ZIP改为持久Export Job：POST只校验当前Asset→Factor→Signal选择与`asset_data_inputs`合法性，随后写入`ops.work_item(work_type='export')`与Signal Job记录并返回202、Status URL。API进程不再构建或驻留大ZIP。
- 独立常驻Signal Export Worker通过租约队列恢复queued/过期running任务；API、浏览器或Worker重启不丢任务。`start-v021-workers.ps1`现同时启动Experiment、Monitoring与Signal Export三个独立进程。
- 一个请求指纹只复用active或未过期且文件验证通过的completed Job；双击不会重复生成大包。文件丢失、size/hash不符或已过期时不复用旧Job，允许建立新Job重建。
- ZIP存入本地内容寻址目录，DB只保存严格`signal-export://sha256/<hash>.zip`、size、schema、文件名与生命周期。下载前重新校验DB引用、路径边界、文件size与SHA-256，任一失败都拒绝返回；通过FastAPI `FileResponse`流式传输。
- Export Result默认14天过期并记录`last_accessed_at`，为后续Storage Maintenance清理提供可审计依据。本轮不执行生产迁移、历史导出或真实删除。
- ZIP成员时间戳、权限、顺序与JSON排序已固定，Manifest不再写入`cache_hit`这类运行态，相同物化内容重建得到相同bytes/hash。
- Signal页提交后轮询queued/running/completed/failed/cancelled阶段，显示中英文状态和进度；completed才启动下载，failed/cancelled显示原因并允许重试。导出内容仍严格只包含已选Signal及独立Target标签，不加入未选Asset/Factor原始数据。
- 已知后续优化：API内存高峰已解除，但Worker内部仍一次构建ZIP bytes；超大研究包将在后续改为Worker直接流式写入临时文件后原子发布。

**异步导出实现信号：`V021-PERSISTENT-SIGNAL-EXPORT-2026-08-09`。**

## 2026-08-09：Cell Result Payload 外置与可回滚维护

- 新生成的Predictive/Portfolio Cell将大体积`series/diagnostics`写入内容寻址Zstd Parquet；PostgreSQL仅保存指标、必要结构/质量摘要、URI、SHA-256、格式、Schema与字节数。历史内联JSONB继续透明读取，本轮没有回填或改写历史结果。
- Worker以带`work_item_id`的pending marker覆盖“文件已发布、数据库事务尚未提交或回滚”的窗口；Producer与Sweeper共享跨进程目录锁，避免并发维护误收刚生成的文件。
- 维护计划冻结完整文件快照并绑定plan hash与精确确认token。执行时在同一目录锁内重新查询全部DB引用、活动Work Item、Product和当前Suite，并复验marker、路径、mtime、size及SHA-256；任一不一致均fail-closed。
- 回收仅移动到`.maintenance-quarantine/<plan_id>`并生成receipt，不直接永久删除；rollback严格禁止覆盖现存文件。提交前崩溃产生的陈旧无引用payload+marker可在宽限期后隔离；提交后遗留marker只隔离marker，已被DB引用的payload继续保留。
- 迁移`20260809_46_v021_cell_payload`只增加nullable引用列；旧行保持NULL并继续读取内联内容。API与三个Worker必须共享同一`STYLE_ROTATION_CELL_RESULT_DIRECTORY`。

## 2026-08-09：Product精确证据二次收紧

- Product Promotion全过程取得与Suite发布/Retention相同的session advisory guard，关闭“Qualification已提交但Product Version尚未发布”时被并发清理的窗口。
- Qualification冻结`product_exact_selection_v1`：共同Asset/Data Input/Factor/Signal/Frequency加唯一选中Model、Target、Slot、Model参数、Strategy与Rule Graph；不再保存兄弟Model/Strategy或`candidate_branches`。
- Product证据的权威来源改为已发布Qualification Artifact的不可变Dependency：恰好6个`qualification_result`与1个`qualification_predictive_result`。系统逐项验证Result类型、来源Suite、选中Strategy/Model及关系数组一致性；任何异常整Suite fail-closed。
- History与Storage共用同一证据解析器，只pin上述7个Cell/Result。额外失败Predictive、兄弟策略、兄弟模型及其Work Item可被回收；Product详情也只展示真正被选中的研究链。

**精确证据并发实现信号：`V021-IMMUTABLE-QUALIFICATION-EVIDENCE-2026-08-09`。**

## 2026-08-09：Runs、双语与前端状态闭环

- Runs 不再是预留空壳；页面只对拥有真实 `research_suite_id` 的 v0.21 Suite 查询进度，绝不把 Lineage Artifact ID 当作队列 Suite ID，并可回到对应 Experiment 查看状态。
- Workspace 对旧浏览器草稿和旧 API 响应保持向后兼容；即使 `asset_data_inputs` 或 `asset_data_input_blockers` 缺失也会使用安全默认值，不再导致首页空白。草稿冲突、dirty submit 和 frequency 双状态均由显式状态机处理。
- Product 详情的请求失败会重试正确 query；推荐只在“持仓决策”页签轮询。Product/Experiment/Runs 的固定文案完成中英双语，初始化同步 `html.lang`。
- 小屏主导航改为可访问的抽屉菜单；Dialog 支持初始焦点、焦点约束、Escape 关闭与关闭后还焦。实验筛选统一重置页码。
- 前端最终通过 TypeScript、ESLint、Production Build 与 21 个 Vitest 用例；真实浏览器逐页复验 Workspace、Assets、Factors、Signals、Models、Strategies、Experiments、Products 与 Runs。
- 真实 Product“持仓决策”复验额外捕获 PostgreSQL 聚合查询遗漏 Calendar Version 分组的 500；补齐 `GROUP BY calendar.calendar_version_id` 后，接口返回 `accepted` 并生成 10 个最新建议持仓，定向单测、Ruff 与 strict mypy 通过。

**前端状态闭环信号：`V021-FRONTEND-STATE-CLOSURE-2026-08-09`。**

## 2026-08-09：正常用户流水线稳定性验收

- 本轮停止以边角契约审计代替用户流程验收，改为连续执行真实的`Asset → Factor → Signal → Model → Strategy → Experiment → Qualification`主链；生产主库不创建测试Product，最终Promotion与Product详情在隔离PostgreSQL测试库完成真实写入验收。
- 修复Predictive与Portfolio队列竞态：API不再启动临时消费者，常驻Experiment Worker成为唯一消费者；Portfolio只有在同Suite、同Model的Predictive Result已被接受后才允许领取。等待中的Portfolio保持`queued / attempt=0`，不会在预测尚未完成时耗尽重试。
- 修复连续第二次实验提交500：旧Retention曾删除Execution Policy与Compiled Spec/Model/Strategy的轻量typed row，却保留不可变Lineage Artifact；后续合法复用Artifact时无法找到typed row。现在Retention只删除Suite/Cell/Result/Work大载荷，并在历史tombstone场景下以事务级advisory lock确定性重建缺失typed row。
- 修复Product资格接口422：Qualification响应Schema补齐Predictive Result/Cell证据ID；同时Experiment Overview按`research_suite_id`精确隔离，不再把当前实验与旧Product固定证据混在一起，失败Cell即使没有Result Artifact也会显示attempt与`error_summary`。
- 新增可重复验收脚本`scripts/validate_v021_user_pipeline.py`，覆盖草稿保存、Compile Preview、探索性Suite提交、状态轮询、逐Cell详情、Portfolio NAV/指标及Promotion Qualification，并将逐Cell证据写入`.codex_work/pipeline-validation/`。

### 真实生产数据矩阵

| 用例 | 资产/研究配置 | Suite | 结果 |
|---|---|---|---|
| A | 4 ETF；动量；K=2 | `ba012822-52d3-435d-980c-10c3c28e84de` | 7/7 accepted，6/6可升级 |
| B | 112只股票；W20+W60双动量；线性等权；K=10 half-k | `450dbd8c-41c5-434a-8944-1d63543dc5da` | 7/7 accepted，6/6可升级 |
| C | 112只股票；偏度+峰度；Dimension Equal；H21；K=20防御 | `954cb740-c742-4f76-8162-cc9bde253bf8` | 7/7 accepted，6/6可升级 |
| D | 4 ETF；低波；K=1；固定20%防御 | `06cab162-c662-4c7b-bf6e-43d346f75b71` | 7/7 accepted，6/6可升级 |

- 汇总：28/28 Cell接受，28/28均一次执行成功，24/24 Portfolio Qualification合格，0失败、0重试；每个策略的近1年、近3年、全历史窗口分别生成246、750、5,023个完整NAV观测。
- 双消费者慢Predictive PostgreSQL回归确认：预测完成前6个Portfolio全部保持queued/attempt=0；预测发布后6个Portfolio均一次完成。
- 隔离PostgreSQL真实Promotion回归确认：合格Result可升级Product，并可读取冻结回测、Asset/Factor/Signal/Model/Target/Strategy精确血缘与Product详情。
- 可见UI验收确认：最新D Suite只显示自身7个单元且0失败；详情三张曲线、完整指标、质量检查、决策解释器和升级入口正常；既有Product的冻结回测、研究配置、样本外状态及最新建议持仓均可读取。
- 前端TypeScript、ESLint、23项Vitest和Production Build通过；本轮相关Python Ruff、strict mypy、队列/提交/恢复PostgreSQL集成测试全部通过。

**正常流水线稳定验收信号：`V021-HAPPY-PATH-STABLE-2026-08-09`。**

## 2026-08-09：可点击前端与本地服务稳定性复验

- 修复本地服务生命周期：API、Experiment Worker、Monitoring Worker 与 Signal Export Worker 统一由脱离终端生命周期的启动器管理；启动状态记录 PID 与启动时间，重复启动会复用健康实例，不会生成重复进程。连续 64.7 秒的 13 次探针全部返回 HTTP 200。
- 修复 Workspace 相同内容的无效自动保存：草稿仅在服务端水合完成且固定字段指纹发生真实变化后才进入 dirty 状态。真实浏览器连续硬刷新后，草稿保持 `v118`，没有再生成空操作 revision。
- 用户草稿已精确恢复为：112 个资产、4 个因子、4 个信号、1 个模型、1 个 Target、2 个策略、周频；工作台 Compile Preview 显示 112/112 可用、1 个模型实例、2 个策略分支、12 个 Portfolio Cells、0 个无效选择。
- 真实浏览器逐层复验 Assets、Factors、Signals、Models、Strategies：上游选择在下游正确继承；Factor 与 Signal 均完成一次取消、自动保存和恢复；恢复后再次按原始顺序写回并锁定为草稿 `v118`。
- 真实点击 Signal 批量导出并等待异步任务完成：Job `bc32d1e6-b310-4a95-a01b-067ea759e8cc` 一次执行成功，ZIP 8,518,146 bytes，SHA-256 为 `c1317c53427907ce1fb73ece161c8f5b97a1858ff4ad418b4be5986215cceb43`。包内仅含 `signals.parquet`、`targets.parquet`、`manifest.json` 与 `data_dictionary.json`；覆盖 112 个资产、4 个已选信号、508,514 行 Signal 与 103,303 行 Target，不包含未选原始因子数据。
- 修复 Product 详情响应过大：v0.21 冻结回测 NAV 曲线从 5,023 点按显示分辨率下采样到最多 600 点并保留首尾，真实响应约从 845.8KB 降至 134.5KB，减少约 84%。
- 当前页面额外延迟的首要外因已经定位为另一个用户任务的约 24 个并行回测进程持续占用约 19 个 CPU 核；候鸟普通 API 在热态仍为毫秒级，未发现失控轮询。未终止、降级或修改该外部任务。
- 最终共享代码树验证：前端 TypeScript、ESLint、24 项 Vitest 与 Production Build 全部通过；后端 API/Workspace/Signal Export 定向单元测试 30 项通过，相关 Ruff 与 strict mypy 通过。

**可点击前端稳定复验信号：`V021-CLICKABLE-UI-STABLE-2026-08-09`。**

## 2026-08-09：固定防御预算精度缺陷与原配置闭环复验

- 对“正常流水线稳定验收”作出明确更正：此前 A–D 四组矩阵证明的是四个抽样配置可运行，不能外推为所有已发布参数组合均已覆盖。特别是没有覆盖“非单位风险预算 × Top-K 边界并列分摊”的组合；因此 `V021-HAPPY-PATH-STABLE-2026-08-09` 只保留为抽样矩阵记录，不再作为完整稳定性结论。
- 用户实际 Suite `8c5748e6-5259-41bf-b136-a92a7349a97f` 精确复现了问题：Predictive 与无防御策略六格完成，但 `us_large_cap_top_k__k10__fixed_20__half_k__none` 的全历史、近 3 年、近 1 年 × 5/10 bps 共六格均在第三次尝试后失败，统一错误为 `Target asset and reserve weights must sum to one`。
- 读取该 Suite 的真实 Predictive 外置载荷和数据库策略配置重放 1,042 个周频决策后，首个坏点定位为 2006-10-13：K=10 时 8 个完整槽位与 3 个边界并列资产分摊剩余 2 个槽位；`risk_budget=0.8` 经 Decimal 分摊后合计为 `0.7999999999999999999999999999`，再加 `reserve=0.2` 后比 1 少 `1E-28`。严格会计契约正确拒绝了该不平衡输入。
- Top-K 在完成槽位精确平衡后，对非单位风险预算缩放产生且绝对值不超过 `1e-18` 的纯算术残差进行确定性回填；仅最小资产键接收 Decimal 尘埃，不改变排名、边界并列份额、风险/防御经济预算或任何阈值语义。新增回归测试精确覆盖 8 个完整槽位 + 3 个并列资产、K=10、half-k、20% 防御的真实排列。
- 修复后以用户当前草稿 revision 122 原样提交：112 个资产、4 个因子实例、2 个信号、1 个模型、1 个 H5 横截面 Target、2 个策略分支、周频。新 Suite `95d74dc9-404b-484c-b5a6-4e05bf73dc5f` 共 13 个 Cell，最终 13/13 accepted、0 failed、全部 attempt=1；其中 Predictive 1/1、fixed_20 防御六格 6/6、无防御六格 6/6。12 个 Portfolio 的 Qualification API 全部 `eligible=true`。
- Product 冻结回测详情继续只读已保存 Result 与外置 Parquet，不调用 Experiment Worker，也不会生成 Work Item。体感上的“重新回测”来自持仓决策 GET：旧实现首次请求读取约 201 万行全历史 Signal，只为计算最新一次建议。现已改为只读取决策日前 14 天的既有 Signal 日期切片，且 `cached_signals_only=true`，缺少缓存时禁止 GET 触发全历史重算。
- 真实 Product `566fec09-a5d7-4b6a-b09d-3f318ae4ae8f` 验证：推荐冷请求约 52.9 秒降至 11.9 秒，热请求约 0.25 秒；请求前后 `ops.work_item` 保持 39→39，Signal 缓存文件数量、大小与 mtime 均无变化。剩余冷耗时来自读取行情完成当前防御判断，不是历史 Portfolio 回测。
- 本地四服务启动器由会被工具宿主回收的 WMI 创建方式改为隐藏 `Start-Process`；API、Experiment Worker、Monitoring Worker 与 Signal Export Worker 使用精确 PID/启动时间复用，当前跨独立调用健康检查通过。未终止或修改另一个项目持续占用约 19 个 CPU 核的并行回测任务。
- 相关验证：Top-K/Execution/Accounting 定向单元 21 项通过；Product Recommendation/Monitoring/Materialization 定向 8 项通过；本轮共享树补丁再次运行 13 项定向测试通过，Ruff 与 strict mypy 通过；真实原配置重跑与 12 项资格查询全部通过。

**固定防御预算与原配置闭环复验信号：`V021-DEFENSE-BUDGET-PIPELINE-VERIFIED-2026-08-09`。**
