const id=location.pathname.split('/').filter(Boolean).pop();const e=id=>document.getElementById(id);const td=(v,f=SR.num)=>`<td class="${SR.tone(v)}">${f(v)}</td>`;
async function load(){try{const data=await SR.get(`/api/v1/factors/${id}`),f=data.factor;e('title').classList.remove('loading');e('title').textContent=f.factor_name;e('family').textContent=`${f.family} / ${f.variant_key}`;e('subtitle').textContent=f.description;e('context').textContent=SR.context(data.context);e('definition').innerHTML=[['计算式',f.formula],['方向',f.direction],['参数',JSON.stringify(f.parameters)],['所需字段',f.required_fields.join(', ')],['最少观测',f.minimum_observations]].map(x=>`<dt>${x[0]}</dt><dd>${x[1]}</dd>`).join('');e('diagnostics').innerHTML=data.diagnostics.map(d=>`<tr><td>${d.frequency}</td><td>${d.period_count}</td>${td(d.mean_rank_ic)}${td(d.positive_ic_ratio,SR.pct)}${td(d.mean_top_bottom_return_spread,SR.pct)}<td>${d.undefined_ic_count}</td></tr>`).join('');e('runs').innerHTML=data.runs.map(r=>{const m=r.metrics;return `<tr><td><a href="/compare?run_ids=${r.run_id}">${r.frequency} · ${r.strategy_template}</a></td><td>${r.transaction_cost_bps} bps</td>${td(SR.metric(m,'strategy.net.cagr'),SR.pct)}${td(SR.metric(m,'strategy.net.sharpe_ratio'))}${td(SR.metric(m,'strategy.net.maximum_drawdown'),SR.pct)}${td(SR.metric(m,'strategy.net.sortino_ratio'))}${td(SR.metric(m,'strategy.net.calmar_ratio'))}${td(SR.metric(m,'strategy.net.cumulative_transaction_cost'),SR.pct)}</tr>`}).join('');const colors=['#59a8ff','#49d6c6'];const series=data.diagnostics.map((d,i)=>({color:colors[i],points:d.periods.map(p=>({value:p.rank_ic})).filter(p=>p.value!=null)}));e('diagLegend').innerHTML=data.diagnostics.map((d,i)=>`<span><i class="dot" style="background:${colors[i]}"></i>${d.frequency}</span>`).join('');SR.draw(e('diagChart'),series)}catch(err){e('error').innerHTML=`<div class="notice">${err.message}</div>`}}load();window.addEventListener('resize',load);
