let selected=new Set();
const el=id=>document.getElementById(id);
function cell(value,format=SR.num){return `<td class="${SR.tone(value)}">${format(value)}</td>`}
async function loadStatus(){try{const data=await SR.get('/api/v1/status');el('context').classList.remove('loading');el('context').textContent=SR.context(data.context);const cards=[['系统状态',data.state==='healthy'?'正式结果完整':'需要检查',data.state],['正式回测',data.counts.publications,''],['因子诊断期',data.counts.diagnostic_periods.toLocaleString(),''],['指标记录',data.counts.metrics.toLocaleString(),'']];el('status').innerHTML=cards.map(x=>`<article class="card"><div class="eyebrow">${x[0]}</div><div class="stat-value ${x[2]}">${x[1]}</div></article>`).join('')}catch(e){el('status').innerHTML=`<div class="notice">${e.message}</div>`}}
async function load(){const qs=new URLSearchParams({frequency:el('frequency').value,strategy_template:el('template').value,cost_bps:el('cost').value,sort_metric:el('sort').value});el('rows').innerHTML='<tr><td colspan="11" class="empty loading">读取结果…</td></tr>';try{const data=await SR.get(`/api/v1/leaderboard?${qs}`);el('rows').innerHTML=data.items.map(item=>{const m=item.metrics,d=item.diagnostic;return `<tr><td><input type="checkbox" aria-label="选择 ${item.variant_key}" data-run="${item.run_id}" ${selected.has(item.run_id)?'checked':''}></td><td><a class="factor-name" href="/factors/${item.factor_variant_id}">${item.factor_name}</a><div class="sub">${item.variant_key} · <span class="pill">${item.family}</span></div></td>${cell(SR.metric(m,'strategy.net.cagr'),SR.pct)}${cell(SR.metric(m,'strategy.net.sharpe_ratio'))}${cell(SR.metric(m,'strategy.net.maximum_drawdown'),SR.pct)}${cell(d.mean_rank_ic,SR.num)}${cell(d.positive_ic_ratio,SR.pct)}${cell(d.mean_top_bottom_return_spread,SR.pct)}${cell(SR.metric(m,'strategy.cost_independent.annualized_turnover'),SR.num)}${cell(SR.metric(m,'strategy.net.cumulative_transaction_cost'),SR.pct)}${cell(SR.metric(m,'strategy.cost_independent.average_reserve_weight'),SR.pct)}</tr>`}).join('')||'<tr><td colspan="11" class="empty">当前筛选没有正式结果</td></tr>';bindChecks()}catch(e){el('rows').innerHTML=`<tr><td colspan="11" class="empty negative">${e.message}</td></tr>`}}
function bindChecks(){document.querySelectorAll('[data-run]').forEach(box=>box.addEventListener('change',event=>{const id=event.target.dataset.run;if(event.target.checked&&selected.size>=4){event.target.checked=false;return}event.target.checked?selected.add(id):selected.delete(id);const button=el('compare');button.disabled=!selected.size;button.textContent=`对比 ${selected.size} / 4`}))}
['frequency','template','cost','sort'].forEach(id=>el(id).addEventListener('change',load));el('compare').addEventListener('click',()=>location.href=`/compare?${[...selected].map(id=>`run_ids=${id}`).join('&')}`);loadStatus();load();
