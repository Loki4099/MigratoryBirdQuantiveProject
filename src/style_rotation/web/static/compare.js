const e=id=>document.getElementById(id),params=new URLSearchParams(location.search),ids=params.getAll('run_ids'),colors=['#59a8ff','#49d6c6','#f2bd5a','#ff7185'];const specs=[['strategy.net.cagr','净 CAGR','pct'],['strategy.net.annualized_volatility','年化波动','pct'],['strategy.net.sharpe_ratio','Sharpe','num'],['strategy.net.sortino_ratio','Sortino','num'],['strategy.net.maximum_drawdown','最大回撤','pct'],['strategy.net.calmar_ratio','Calmar','num'],['strategy_vs_four_etf_equal_weight.net.tracking_error','Tracking Error','pct'],['strategy_vs_four_etf_equal_weight.net.information_ratio','Information Ratio','num'],['strategy.cost_independent.annualized_turnover','年化换手','num'],['strategy.net.cumulative_transaction_cost','累计成本','pct'],['strategy.cost_independent.average_reserve_weight','平均储备权重','pct']];
async function load(){if(!ids.length){e('error').innerHTML='<div class="notice">请先从排行榜勾选 1–4 个回测。</div>';return}try{const query=ids.map(x=>`run_ids=${encodeURIComponent(x)}`).join('&'),data=await SR.get(`/api/v1/compare?${query}`);e('context').textContent=SR.context(data.context);e('legend').innerHTML=data.items.map((x,i)=>`<span><i class="dot" style="background:${colors[i]}"></i>${x.variant_key} · ${x.frequency} · ${x.strategy_template} · ${x.transaction_cost_bps}bps</span>`).join('')+'<span><i class="dot" style="background:#8fa4b8"></i>SPY 买入持有</span>';const series=data.items.map((x,i)=>({color:colors[i],points:x.nav.map(p=>({value:p.net_nav}))}));if(data.items[0]?.spy?.length)series.push({color:'#8fa4b8',points:data.items[0].spy.map(p=>({value:p.net_nav}))});SR.draw(e('navChart'),series);const header=data.items.map(x=>`<th>${x.variant_key}<div class="sub">${x.frequency} · ${x.strategy_template} · ${x.transaction_cost_bps}bps</div></th>`).join('');const rows=specs.map(([key,label,kind])=>`<tr><td>${label}</td>${data.items.map(x=>{const v=SR.metric(x.metrics,key),format=kind==='pct'?SR.pct:SR.num;return `<td class="${SR.tone(v)}">${format(v)}</td>`}).join('')}</tr>`).join('');e('metrics').innerHTML=`<table><thead><tr><th>指标</th>${header}</tr></thead><tbody>${rows}</tbody></table>`}catch(err){e('error').innerHTML=`<div class="notice">${err.message}</div>`}}load();
